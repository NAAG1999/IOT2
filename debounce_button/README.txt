Code to blink led when button is pressed one time, and on pressing it again, led stops blinking.
Different from using general nestes if else loops.

Uses mili() function to record the time stamps.

Records time from the instant arduino is supplied voltage.
